import React from 'react';
import PropTypes from 'prop-types';
import './FormComp.css';

const Form = () => (
  <div className="Form">
    Form Component
  </div>
);

Form.propTypes = {};

Form.defaultProps = {};

export default Form;
